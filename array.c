#include <stdio.h>
#include <cs50.h>

int main (void)
int i = 1;

{
    int size;
    do
    {
        size = get_int("Enter size: ");
    }
        while (size < 1);

    int array[size];

    for (i = 0; i < size; i++)
    {
        array{i} = 2 * array (i -1)
        printf("%i\n", array{i});
    }


    //get array size

    for (array = 1; array <= size; array++)

    array{size};


}