#include <stdio.h>
#include <cs50.h>

int main (void)
int size;

{

    do
    {
        size = get_int("Enter size: ");
    }
        while (size < 1);

    //printf()

    int array[size];


    //get array size

    for (array = 1; array <= size; array++)

    array{size};


}