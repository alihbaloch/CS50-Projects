#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    if((argc == 0) || (argc > 1))
    {
        printf("Usage: ./caesar key\n");
        return 1;
    }
    else
    {
        return 0;
    }
}