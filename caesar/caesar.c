#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    if((argc == 1) || (argc > 3))
    {
        printf("Usage: ./caesar key\n");
        return 1;
    }
    else
    {
        return 0;
    }
}